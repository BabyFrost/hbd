package com.uba;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.Month;
import java.time.Period;

public class DifferenceBetweenDates {

	/*
	 * public static void main(String[] args) { LocalDate bday = LocalDate.of(2021,
	 * Month.JULY, 20); LocalDate today = LocalDate.now(); Period age =
	 * Period.between(bday, today); int years = age.getYears(); int months =
	 * age.getMonths(); int days = age.getDays();
	 * 
	 * System.out.println("number of days: " + days);
	 * System.out.println("number of years: " + years);
	 * System.out.println("number of months: " + months);
	 * 
	 * }
	 */
}
